apiVersion: v1
kind: Service
metadata:
  name: pipeline-monitor-service
spec:
  type: NodePort
  selector:
    app: pipeline-monitor
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30007